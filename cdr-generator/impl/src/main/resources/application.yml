spring:
    application:
        name: cdr-generator
    datasource:
        url: jdbc:h2:mem:cdrdb
        username: su
        password: 
        driver-class-name: org.h2.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.H2Dialect
